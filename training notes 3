Git hub----
push
clone
pull
merge

see the comments
1.clone
    create one repo,and push the files...in local system create the folder...and in gitbash config the path,username,user email
commands--git config --global user.name ""
          git config --global user.name ""
          git init
          see status---git status
          git add .---add files
          if to commit---git commit -m "msg"
          push=---git push -u origin (name)
          to clone
              go to the git bash of the path...do all above steps then git clone -b (master/main/branch) path of repo
2.merge/pull
      create the branch---git branch name
then git checkout name
commit anything----compare and pull in github and ,merge it.

----------------------------------------------------------
java
scp(string const pool),heap
1.polymorphism
2.down and up casting
3.factory method
4.static method and variables
5.arrays
6.strings-concatenation
7.string handling,hashcode,hashset,mthods,serializable---stream to object,comparable,charseq
8.diff btw string buffer-one thread can update the and write operation by one,write ope is locked thread safety,slow
string builder--
9.string buffer,string classes
10.abstract and interface
11.java string class


hw-thread safety and non thread safety

topics
after servelts and jsp test-80%pass (40 questions)
